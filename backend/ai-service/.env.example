# Google Gemini API key — required
GEMINI_API_KEY=
# Shared secret required in the X-API-Key header of every /extract and /match call
AI_SERVICE_API_KEY=
# Optional: comma-separated browser origins allowed to call the service directly
ALLOWED_ORIGINS=
